= simple_form_for [:admin, @distributor], html: { class: 'form-horizontal' } do |f|
  = f.input :title
  = f.input :address
  = f.input :contacts

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
    = link_to t('.cancel'), admin_distributors_path, class: 'btn'
