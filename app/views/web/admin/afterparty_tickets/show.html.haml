.page-header
  %h1=t '.title'

.span6.clearfix
  %table.table.table-hover
    %thead
      %tr
    %tbody
      %tr
        %th= AfterpartyTicket.human_attribute_name :id
        %td= @ticket.id
      %tr
        %th= AfterpartyTicket.human_attribute_name :order
        %td= link_to @ticket.order.id, admin_order_path(@ticket.order)
      %tr
        %th= AfterpartyTicket.human_attribute_name :user
        - if @ticket.user
          %td= link_to @ticket.user, admin_user_path(@ticket.user.id)
        - else
          %td
      %tr
        %th= AfterpartyTicket.human_attribute_name :ticket_code
        %td= @ticket.ticket_code.try(:code)
      %tr
        %th= AfterpartyTicket.human_attribute_name :price
        %td= @ticket.price

.form-actions
  = link_to t('.back'), admin_afterparty_tickets_path, :class => 'btn'
