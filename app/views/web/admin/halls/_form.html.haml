= simple_form_for [:admin, @hall], :html => { :class => 'form-horizontal' } do |f|

  = f.input :title      

  .form-actions
    = f.button :submit, :class => 'btn btn-primary'
    = link_to t('.cancel'), admin_halls_path, :class => 'btn'
