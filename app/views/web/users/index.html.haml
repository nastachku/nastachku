- model_class = User
.page-header
  %h1=t '.title'
  - @users.each do |user|
    - line = []
    - line << user.name
    - line << user.company  if user.company.present?
    - line << user.position if user.position.present?
    = line.join ", "
    %br/
